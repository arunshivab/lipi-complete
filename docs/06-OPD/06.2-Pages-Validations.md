# 06.2 — Outpatient Department: Pages & Validations

> **Module**: Outpatient Department  
> **Status**: Spec Only  
> **CSS Prefixes**: op-,ov-

---

## VALIDATION TIMING (STANDARD — applies to all forms)

```
On first focus → No validation
On first blur → Validate field, show error if any
On subsequent input → Re-validate (silent if no error, show if error)
On Save click → Validate ALL fields, scroll to first error if any
```

---

## ACCESSIBILITY (a11y) RULES

Every input/select/textarea MUST have:
- `id="<prefix>-<fieldName>"` (e.g., for module: `op-fieldName`)
- `name="<camelCase>"`
- `autocomplete="<valid-value>"` or `autocomplete="off"`
- `<label for="...">`

---

## SAVE BEHAVIOR (STANDARD)

```
User clicks "Save":
  1. Validate all fields
  2. If errors → scroll to first, focus, abort
  3. Begin transaction
  4. INSERT/UPDATE in DB (per .3 schema)
  5. INSERT audit_event (use AuditActions constant)
  6. Commit transaction
  7. Show success near Save button
  8. Disable form (fieldset disabled)
  9. Save → "✓ Saved", Cancel → "Close"
```

---

## CONFIRMATION DIALOGS

Required for ALL destructive/irreversible actions:
- Delete, Lock, Suspend, Reactivate, Status changes, etc.

```
┌──────────────────────────────────────────┐
│ [Action]: [Record name]?                  │
├──────────────────────────────────────────┤
│ This will:                                │
│ • [Consequence 1]                         │
│ • [Consequence 2]                         │
│                                            │
│ [Cancel]              [Confirm Action]   │
└──────────────────────────────────────────┘
```

---

## FORM FIELDS (define when module built)

### Page: [PageName]

| Field | Type | Required | Validation | id | name | autocomplete |
|-------|------|----------|-----------|----|----|--------------|
| TODO | TODO | TODO | TODO | TODO | TODO | TODO |

---

## AUDIT EVENTS (define module-specific codes)

| Action | Event Code | What's Captured |
|--------|-----------|----------------|
| Create | `06_CREATED` | All field values |
| Update | `06_UPDATED` | before_state, after_state |
| Delete (soft) | `06_DELETED` | reason |

> **Note**: Event codes follow uppercase, underscore_separated convention.

---

## REFERENCES

- **Design**: `06.1-Design-Specs.md`
- **Database**: `06.3-Database-Schema.md`
- **Audit Service**: `src/LiPi.Web/Services/AuditService.cs`
- **Action Constants**: `AuditActions` static class

---

> **Status**: SPEC TEMPLATE. Fill in field-by-field validation rules during active development.
